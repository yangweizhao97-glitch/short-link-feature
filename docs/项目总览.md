# 短链接项目总览

## 当前状态

当前目录包含短链接功能的规格、API 设计、数据库设计、代码实现、验证记录、Review 记录和工作记录。

该项目目前是一个 TypeScript 短链接后端原型，包含可启动 HTTP 服务、接口处理函数、JSON 文件仓储、自动化接口测试和场景脚本。当前实现了：

1. `POST /api/short-links` 的创建逻辑。
2. `GET /:code` 的跳转逻辑。
3. JSON 文件仓储作为本地开发替身。
4. `tests/api` 下的后端接口测试。

## 代码文件

| 文件 | 说明 |
|---|---|
| `src/server.ts` | HTTP 服务入口和可测试 server factory |
| `src/short-links/index.ts` | 短链接模块统一出口 |
| `src/short-links/domain/generateShortCode.ts` | 生成短码，默认 6 位，字符集为大小写字母和数字 |
| `src/short-links/domain/validateShortLinkUrl.ts` | 校验长链接 URL，限制协议和部分内网地址 |
| `src/short-links/domain/shortLinkTypes.ts` | 短链接相关类型定义 |
| `src/short-links/repositories/shortLinkRepository.ts` | JSON 文件仓储和仓储接口 |
| `src/short-links/handlers/createShortLink.ts` | 创建短链接处理函数 |
| `src/short-links/handlers/redirectShortLink.ts` | 根据 code 跳转处理函数 |
| `tests/api/short-links.api.test.ts` | 后端接口自动化测试 |
| `tests/api/manual-commands.md` | 手工接口测试命令 |
| `scenarios/generateShortCode.scenario.ts` | 短码生成场景脚本 |
| `scenarios/postShortLinks.scenario.ts` | 创建短链接场景脚本 |
| `scenarios/getShortLinkByCode.scenario.ts` | 短链接跳转场景脚本 |
| `scenarios/fullFlow.scenario.ts` | 创建、跳转、访问计数完整场景脚本 |
| `scenarios/validationFailures.scenario.ts` | URL 和短码异常场景脚本 |

## 文档文件

| 文件 | 说明 |
|---|---|
| `docs/后端开发规格.md` | 后端规格、接口、数据模型、核心流程、异常处理、验收标准、任务拆解 |
| `docs/REST API 设计.md` | REST API 请求参数、响应结构、错误码和边界条件 |
| `docs/数据库模型设计.md` | 数据库表结构、索引、访问统计、过期时间和扩展设计 |
| `docs/验证记录.md` | 当前已执行的验证记录 |
| `docs/短链接404排查记录.md` | `/abc123` 与 `/api/:code` 路由不匹配导致 404 的排查记录 |
| `docs/安全Review.md` | 从安全角度 review 当前短链接功能 |
| `docs/结构化日志设计.md` | 创建失败、跳转失败、访问统计问题的结构化日志字段设计 |
| `docs/当前DiffReview.md` | 根据当前实现相对规格做的 diff review |
| `docs/项目总览.md` | 当前项目文件和状态总览 |
| `docs/接口测试说明.md` | 自动化接口测试和手工接口测试说明 |

## 工作记录

| 文件 | 说明 |
|---|---|
| `docs/工作记录/代码记录.md` | 记录使用过的 skill 和生成的代码文件 |
| `docs/工作记录/文档记录.md` | 记录使用过的 skill 和生成的文档文件 |

## 配置文件

| 文件 | 说明 |
|---|---|
| `package.json` | 项目脚本和 TypeScript 相关依赖 |
| `package-lock.json` | npm 锁文件 |
| `tsconfig.json` | TypeScript 类型检查配置 |

## 本次流程复核结果

### 类型检查

执行命令：

```bash
npm run typecheck
```

结果：

```text
通过
```

### 核心行为验证

使用临时 JSON 仓储验证：

1. 创建短链接返回 `201`。
2. 生成 6 位短码。
3. 跳转返回 `302`。
4. `Location` 指向原始地址。
5. 成功跳转后 `clickCount = 1`。

结果：

```text
通过
```

### 依赖安全审计

执行命令：

```bash
npm audit --audit-level=high
```

结果：

```text
未完成
```

原因：

```text
npm audit 请求 registry 时 TLS 连接失败。
```

## 已知问题

当前 Review 已记录以下重点问题：

1. 已禁用短链接仍会跳转。
2. 访问计数不是原子更新。
3. 缺少创建接口和跳转接口频率限制。
4. 短码格式过宽，允许任意长度。
5. URL 校验无法拦截解析到内网 IP 的域名。
6. 跳转使用 `originalUrl`，没有使用 `normalizedUrl`。
7. TypeScript 使用 dev 版本，建议改为稳定版本。

详细内容见：

1. `docs/安全Review.md`
2. `docs/当前DiffReview.md`

## 下一步建议

建议按以下顺序推进：

1. 修复 `GET /:code` 对 `status !== "active"` 的处理。
2. 限制短码长度为 6 到 8 位。
3. 给创建和跳转接口设计并实现限流。
4. 把跳转目标从 `originalUrl` 调整为 `normalizedUrl`。
5. 将 JSON 仓储替换为真实数据库仓储，访问计数使用原子更新。
6. 补充结构化日志实现，遵守 `docs/结构化日志设计.md`。
7. 在网络可用时重新执行 `npm audit --audit-level=high`。
