# 文档记录

1. 用了 `spec-driven-development` skill，写成了 `docs/后端开发规格.md` 文件。
2. 用了 `planning-and-task-breakdown` skill，写成了 `docs/后端开发规格.md` 文件中的增量开发任务拆解。
3. 用了 `api-and-interface-design` skill，写成了 `docs/REST API 设计.md` 文件。
4. 用了 `api-and-interface-design` 和 `security-and-hardening` skill，写成了 `docs/数据库模型设计.md` 文件。
5. 用了 `debugging-and-error-recovery` skill，写成了 `docs/短链接404排查记录.md` 文件。
6. 用了 `security-and-hardening` 和 `code-review-and-quality` skill，写成了 `docs/安全Review.md` 文件。
7. 用了 `observability-and-instrumentation` skill，写成了 `docs/结构化日志设计.md` 文件。
8. 用了 `code-review-and-quality` 和 `security-and-hardening` skill，写成了 `docs/当前DiffReview.md` 文件。
9. 用了 `documentation-and-adrs` skill，写成了 `docs/项目总览.md` 文件。
10. 用了 `test-driven-development`、`debugging-and-error-recovery` 和 `performance-optimization` skill，写成了 `docs/测试与优化流程复核.md` 文件。
